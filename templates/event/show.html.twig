{% extends 'base.html.twig' %}

{% block title %}{{ event.title }}{% endblock %}

{% block body %}

    <section class="relative">
        <img src="{{ event.image }}"
             class="w-full h-[420px] object-cover">

        <div class="absolute inset-0 bg-gradient-to-t from-black/70 to-transparent"></div>

        <div class="absolute bottom-0 left-0 right-0">
            <div class="max-w-7xl mx-auto px-6 py-10 text-white">
                <h1 class="text-4xl font-extrabold mb-2">
                    {{ event.title }}
                </h1>
                <p class="text-indigo-100">
                    📍 {{ event.location }} · 🕒 {{ event.date|date('F j, Y · H:i') }}
                </p>
            </div>
        </div>
    </section>

    <section class="max-w-4xl mx-auto px-6 py-16">
        <div class="bg-white rounded-2xl shadow p-10">

            <p class="text-lg leading-relaxed mb-10 text-slate-700">
                {{ event.description }}
            </p>

            <div class="flex items-center justify-between">
                <div class="text-sm text-slate-500">
                    0 / 0 attending
                </div>

                <button
                    class="px-8 py-4 bg-indigo-600 text-white font-semibold rounded-xl hover:bg-indigo-700 transition">
                    Register now
                </button>
            </div>

        </div>
    </section>

{% endblock %}

{% block footer %}
    {% include 'components/_footer.html.twig' %}
{% endblock %}
